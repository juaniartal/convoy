# Copy this file to .env and fill in the values from your GitHub App.
# Never commit the real .env file — it's already in .gitignore.

# Created automatically the first time you run `npm run dev` (Probot's
# manifest flow walks you through creating the GitHub App in your browser).
APP_ID=
PRIVATE_KEY_PATH=./private-key.pem
WEBHOOK_SECRET=

# Local dev only: forwards GitHub's webhook deliveries to your machine.
# Not used in production — there, GitHub delivers directly to your ingress URL.
WEBHOOK_PROXY_URL=

# Port the server listens on.
PORT=3000

# Optional: if set, all /api/* and dashboard requests must send
# "Authorization: Bearer <value>". Leave unset only if your ingress/SSO
# already restricts who can reach this service.
CONVOY_API_KEY=

# Path to the per-repo classification overrides / exclude list.
CONVOY_CONFIG_PATH=./convoy.yaml
